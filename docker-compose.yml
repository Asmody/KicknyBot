
services:
  kickny-bot:
    build: .
    container_name: kickny-bot
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./.env:/app/.env
    environment:
      - TZ=Europe/Moscow